export function aboutMainContent() {
    const newDiv = document.createElement('div');
    newDiv.innerText = "this is a About Us test";
    return newDiv;
    
    // mainContainer.appendChild(newDiv);
}