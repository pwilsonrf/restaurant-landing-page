export function pricingMainContent() {
    const newDiv = document.createElement('div');
    newDiv.innerText = "this is a Pricing test";
    return newDiv;
    
    // mainContainer.appendChild(newDiv);
}